import React from "react";
import { Link, Outlet } from "react-router-dom";

const Login = () => {
  return (
    <>
      <div>Login</div>
      <Outlet />
    </>
  );
};

export default Login;
